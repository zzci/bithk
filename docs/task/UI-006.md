# UI-006 Remove top stats from projects and ships lists

- **status**: completed
- **priority**: P1
- **owner**: current session
- **createdAt**: 2026-05-25 18:20

## Description

Remove the top KPI/meta stat blocks from the projects and ships list pages.
Keep the existing list filters, search controls, pagination, creation actions,
and card rendering.

Acceptance criteria:

1. `/projects` no longer renders the top project stats strip.
2. `/ships` no longer renders the top ship stats tiles.
3. Existing filter chips, search boxes, pagination, and create actions remain.
4. Focused frontend verification passes for the changed route files.

## ActiveForm

Removing top stats from projects and ships lists.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

2026-05-25 18:20 - Investigation found project stats are rendered by
`StatStrip` / `StatCard` in `projects/index.lazy.tsx`; ship stats are rendered
by `StatTile` in `ships/index.lazy.tsx`. Filter chip counts and pagination
should remain unchanged.

2026-05-25 18:25 - Removed the top stats blocks from both list pages, cleaned
up unused imports and local helpers, and verified the changed routes with
ESLint plus web typecheck.
