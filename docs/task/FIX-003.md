# FIX-003 Drive trash/delete confirmation dialogs

- **status**: done
- **priority**: P2
- **owner**: main
- **createdAt**: 2026-05-24

## Description

Drive 两类破坏性操作此前直接执行 mutation，缺少二次确认（研发阶段，接受破坏性更改）：

1. **移到回收站**（`-file-browser.tsx`）：单项右键/下拉菜单、批量工具栏按钮，
   均直接 `trashEntry.mutate`，无确认。
2. **永久删除**（`-drive-entry-list.tsx`，仅回收站视图）：单项、批量、自定义
   `permanent-delete` 动作均直接 `permanentDelete.mutate`，无确认。

复用既有 `shared/components/ui/confirm-delete-dialog.tsx`（documents/projects 模块的
删除确认范式）。

## ActiveForm

- `-file-browser.tsx`：`DialogState` 增 `{ type: "trash"; ids; name? }`，
  `onDelete`/`onBatchDelete` 改为先 `setDialog`，确认后才循环 mutate。
- `-drive-entry-list.tsx`：增 `confirmDelete` 状态，单项/批量/自定义永久删除
  动作先弹确认，确认后 mutate；文案强调不可恢复。
- i18n：`en/drive.json` + `zh/drive.json` 的 `browser.dialog` 下新增 trash/
  deleteForever 的单项+批量 title/description 键。

## Verify

- `bun run check` 通过
- 手动：文件夹视图单项+批量"移到回收站"弹确认；回收站视图单项+批量"永久删除"弹确认。
