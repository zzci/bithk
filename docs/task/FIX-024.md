# FIX-024 Tag-filter: neutral trigger + hide selected from list

- Status: Completed
- Plan: [PLAN-048](../plan/PLAN-048.md)
- Owner: BKD L3 4tbe0vv8
- Campaign: l1-75ymcfnr-tagfx-20260602025356
- Updated: 2026-06-02

## Goal

Three small tweaks to `ProjectTagFilter`:

1. The selector trigger stays NEUTRAL (outline) regardless of how many tags are
   selected — never the filled `default`/`primary` highlighted variant.
2. The dropdown/combobox list excludes already-selected tags (only unselected
   tags listed); graceful empty state when none remain.
3. Rename the trigger label 更多 → 标签 (en "Tags"): update `list.tagFilterMore`
   value + the `list.tagFilterMoreLabel` aria-label in both locales (parity).

## Scope

- `apps/web/src/app/routes/_app/projects/-project-tag-filter.tsx`
- `apps/web/src/app/routes/_app/projects/-project-tag-filter.test.tsx`
- `apps/web/src/locales/{en,zh}/projects.json` (only if a new empty-state string
  is added; keep en/zh parity)

Out of scope: the 3 consumers (issues tab, procurement tab, projects list
`index.lazy.tsx`) — props must stay valid with NO edits. Do not change the chip
rendering.

## Acceptance

- Multi-select combobox `TagFilterCombobox` trigger never renders the filled
  `primary` variant; remove the `active` prop / its highlight branch — outline
  always.
- Single-select `DropdownMenuTrigger` Button `variant` is always `"outline"`
  (drop the `selected.length > 0 ? "default" : "outline"`).
- Both lists list only UNSELECTED tags: the single-select `DropdownMenuContent`
  map and the multi-select `matches` exclude the selected id(s).
- All-selected / none-remaining renders a graceful empty / "no more tags" state
  (no crash, no empty-flicker) in both modes.
- Public props/signatures UNCHANGED; all 3 consumers compile untouched.
- `-project-tag-filter.test.tsx` updated: trigger never-highlighted; selected
  tags absent from the list. Green.
- `bun run check` exits 0 (mind the known `@milkdown/ctx` flake in
  `-project-issue-panel.test.tsx`).

## Notes

- 2026-06-02 - Created + dispatched by L2 h9ieukwl to L3 4tbe0vv8 (working).
- 2026-06-02 - First L3 attempt produced ZERO commits (engine usage limit).
  Re-dispatched 4tbe0vv8 with expanded 3-part spec (added the 更多→标签 rename).
- 2026-06-02 - L3 4tbe0vv8 green (commit d55b647): single-select Button
  variant="outline" unconditional; multi-select `active` prop removed end-to-end
  + ComboboxTrigger pinned neutral; both lists exclude selected ids; all-selected
  empty state via new `list.tagFilterNoMore` (en/zh); labels tagFilterMore +
  tagFilterMoreLabel → "Tags"/"标签". Consumer SOURCE untouched; only
  -project-issues-tab.test.tsx label-query strings adapted (rename). Merged
  --no-ff into bkd/h9ieukwl (c69f471); final combined `bun run check` EXIT=0.
  Completed.
