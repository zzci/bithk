# UI-001 Redesign ships, projects, and contacts content pages

- **status**: completed
- **priority**: P1
- **owner**: L1/BKD
- **createdAt**: 2026-05-25 15:51

## Description

Update only the content pages for the ships, projects, and contacts modules to
match the prototype bundle under `backup/untitled`.

Acceptance criteria:

1. Ships list and detail content pages follow the prototype's dense operational
   layout while preserving existing data operations.
2. Projects list and detail content pages follow the prototype's project
   management layout while preserving existing tabs and permissions.
3. Contacts content page follows the prototype's directory layout while
   preserving masking, sharing, editing, and deletion behavior.
4. Global sidebar, app shell, unrelated modules, and backend behavior remain
   out of scope.
5. `bun run check` passes after the implementation.

## ActiveForm

Coordinating the ships, projects, and contacts UI redesign through BKD.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

2026-05-25 15:51 - L1 investigation completed. BKD project `bqnuoyra`; L1 issue
`mwo9qmid`. Reference prototype entry is `backup/untitled/README.md`, which
points to `backup/untitled/project/项目管理.html` and its imported files.

2026-05-25 15:52 - Created BKD L2 issue `cw6d4wrk` with `engineType=claude-code`
for campaign `l1-mwo9qmid-20260525155159`. Registered L2 cron
`l2-dispatch-cw6d4wrk` and L1 progress cron `l1-progress-mwo9qmid`.

2026-05-25 16:30 - UI-001 completed through BKD campaign
`l1-mwo9qmid-20260525155159`.

Merged work:

- Prototype map: `c8acba6`
- Contacts redesign: `46e89e9`
- Projects redesign: `ae3f06f`
- Ships redesign: `415209a`
- Integration pass: `64bf7b5`

Verification:

- `bun run check` passed after each module merge and after the final integration
  pass.
- Integration self-review found no P0/P1 issues.
- Accessibility/responsive pass covered filter chip pressed state, decorative
  search icon labeling, keyboard title-cell control, retained semantic tables,
  responsive KPI grids, one-column heroes, sheet width caps, table overflow
  wrappers, and stable FileBrowser height.

Human follow-up:

- Verify the redesigned UI manually, then move BKD issues `vymhypfi`,
  `7gbcblog`, `108qynws`, `t7f861ke`, and `hgnnswbi` from review to done to
  trigger worktree cleanup.
