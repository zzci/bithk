# FIX-056 - Quality-gate dedup, api dev --watch, CI cache, flake retry, lint tests/

- Status: Completed
- Plan: [PLAN-104](../plan/PLAN-104.md)
- Campaign: `l1-bithk-arch-20260702084717`
- Owner: L3
- Created: 2026-07-02

## Summary

The api test suite executes up to 3× per push; the dev loop has a documented stale-backend
failure mode. See AUDIT-20260702-architecture.md → D12.

## Acceptance Criteria

- `check:routes` removed from `check` (root `package.json:36`); the standalone script stays for
  targeted runs.
- CI coverage gate parses coverage from the `check` step's output (coverage is already always-on
  via `apps/api/bunfig.toml`) instead of re-running the full api suite.
- `apps/api` dev script gains `--watch` (verify dev:all still works; no double-start).
- Root `lint` script covers `tests/`; the existing dead eslint override for `tests/e2e/**` now
  fires; lint violations in tests fixed.
- vitest `retry: 1` scoped to the two milkdown-mounting suites (`comment-section.test.tsx`,
  `-documents-create.test.tsx`) or the teardown fixed outright.
- CI: bun install cache (`~/.bun/install/cache` keyed on `bun.lock`).
- `overrides` vs manifest reconciled (hono pin matches resolved version).
- Dead `tests/parity/capture-plan-019.ts` deleted.
- `bun run check` passes locally; CI config changes lint-clean (actionlint if available, else
  review).

## Files in Scope

- root `package.json`, `.github/workflows/ci.yml`, `apps/api/package.json`,
  `apps/web/vitest.config.ts` (or per-suite retry), `eslint.config.ts`, `tests/parity/`.

## Status Notes

- 2026-07-02: Completed — `check:routes` dropped from `check`, CI coverage parsed from the check
  step, api dev gained `--watch`, bun install cache added, milkdown-suite retry scoped, `tests/`
  linted, dead parity script deleted.
