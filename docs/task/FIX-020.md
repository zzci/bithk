# FIX-020 Create-issue dialog refinements

- Status: Completed
- Plan: [PLAN-047](../plan/PLAN-047.md)
- Owner: BKD L3 d282dw34
- Campaign: l1-75ymcfnr-uipolish-20260602015825
- Updated: 2026-06-02

## Goal

Refine `CreateIssueDialog` in
`apps/web/src/app/routes/_app/projects/-project-issues-tab.tsx`:

1. Remove the top breadcrumb bar (`{projectName} › 手动创建`). Keep maximize (⤢)
   + close (×); float them at the dialog top-right corner so both still work.
   Body starts with the title input then description.
2. ⤢ expands the DESCRIPTION editing area (taller textarea), not just the shell.
3. Replace the read-only `项目 / {projectName}` pill (last metadata pill) with a
   working ATTACHMENT button that uploads attachments to the new issue. Remove
   the bottom-left paperclip; the metadata-row attachment button is the single
   attach affordance (reuse the paperclip's existing attachment wiring).
4. Tighten the bottom bar padding (more compact). Footer = [继续创建][创建 issue].

## Scope

- `apps/web/src/app/routes/_app/projects/-project-issues-tab.tsx` (CreateIssueDialog)
- `apps/web/src/locales/{en,zh}/projects.json` — only `issues.*` keys, if any
  string changes (keep parity).

Out of scope: tag filter, procurement, backend, status/priority enums.

## Acceptance

- No breadcrumb bar; ⤢ and × float top-right and both work.
- ⤢ visibly grows the description textarea height.
- Project pill gone; attachment button present and functional; only one
  attachment entry point (paperclip removed).
- Footer compact (toggle + submit); en/zh parity preserved.
- `bun run check` exits 0 (mind the known `@milkdown/ctx` flake).

## Notes

- 2026-06-02 - Created + dispatched by L2 vwfwi45t.
- 2026-06-02 - L3 d282dw34 green (single file; reused existing i18n keys, no
  locale change). L3 forgot to commit; L2 committed its worktree changes
  (310affa) then merged --no-ff into bkd/vwfwi45t (merge 6991c7d). Post-merge
  `bun run check` REAL_EXIT=0. Completed.
