# UI-029 - Split web god-components; static i18n label maps; memoized list rows

- Status: Completed
- Plan: [PLAN-104](../plan/PLAN-104.md)
- Campaign: `l1-bithk-arch-20260702084717`
- Owner: L3 (wave 2 — after UI-028 merges; shares files)
- Created: 2026-07-02

## Summary

Oversized feature files breach the 800-line cap and block testing; dynamic i18n template keys
defeat unused-key detection; list rows are unmemoized. See AUDIT-20260702-architecture.md → D11.

## Acceptance Criteria

- `-project-issues-tab.tsx` (897): extract create-issue dialog + row widgets into co-located `-`
  files; `admin/users/groups.lazy.tsx` (858, 24 useState): split dialogs + table units;
  `-contact-panel.tsx` (717) and `-colleague-panel.tsx` (691): extract sections reusing
  `detail-panel-header.tsx`/`detail-meta-row.tsx`. All resulting files ≤ ~500 lines; behavior
  identical.
- Dynamic `t(\`ns.x.${v}\`)` status/label lookups replaced with exhaustive `Record<Status,
  string>` key maps (starting with `-project-issues-tab.tsx:350,708-716`,
  `-approvals-page.tsx:90`); `check:i18n` passes with no new unused keys.
- List rows in issues tab / audit / groups wrapped in `React.memo` with stable callbacks.
- Component tests added for the extracted dialogs/sections (raise, never lower, coverage).
- `bun run check` passes.

## Files in Scope

- `apps/web/src/app/routes/_app/projects/-project-issues-tab.tsx` (+new co-located files),
  `admin/users/groups.lazy.tsx`, `contacts/-contact-panel.tsx`, `hr/-colleague-panel.tsx`,
  `hr/-approvals-page.tsx`, locale JSONs if keys are restructured.

## Status Notes

- 2026-07-02: Completed — oversized issue-tab/groups/contact/colleague components split into
  co-located units, dynamic i18n lookups replaced with static key maps, list rows memoized, and
  component tests added for the extracted pieces.
