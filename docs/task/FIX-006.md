# FIX-006 Restore original issue detail behavior without UI changes

- Status: Done
- Plan: [PLAN-027](../plan/PLAN-027.md)
- Owner: BKD L2 7yau58uo
- Updated: 2026-05-28

## Goal

Compare the current BITHK project work-order detail implementation with the
original issue detail implementation in `/app/zzci/access`, then migrate any
missing non-UI behavior that can be restored without changing the current
frontend layout, styling, visible text, or interaction patterns.

## Scope

- Current issue/work-order backend routes, services, schema, and tests.
- Current frontend API consumers and route wiring only for investigation or
  non-visible contract/test updates.
- Reference issue detail implementation under `/app/zzci/access`.
- PMA tracking and focused verification.

Out of scope: visible frontend UI changes, project work-order list refinements,
dialog redesign, dependency upgrades, schema changes unless a real reference
behavior gap requires them, and unrelated module refactors.

## Acceptance

- Current and reference issue detail implementations are located and compared.
- Missing detail capabilities are documented before implementation.
- Any migrated work is limited to backend/API/service/model/tests or
  non-visible contract updates.
- Current frontend UI layout, styling, visible text, and interactions are not
  changed.
- Focused tests cover the migrated behavior or confirm parity.
- Focused verification runs and `bun run check` is attempted.

## Notes

- 2026-05-28 16:32 - L2 investigation started for campaign
  `l1-cuu89zau-20260528162836`. The current project exposes project-scoped
  work-order detail routes under `/projects/:projectId/issues/:id`; the
  reference repository exposes the original issue detail routes under
  `/issues/:id`.
- 2026-05-28 16:50 - L3 `nnkhy9ja` confirmed no missing non-UI issue detail
  behavior. Merged test-only parity coverage in `23b342a` for project-scoped
  detail comments and attachments, with `bun run check` passing.
