# UI-007 Remove top stats from contacts list

- **status**: completed
- **priority**: P1
- **owner**: current session
- **createdAt**: 2026-05-25 18:30

## Description

Remove the top KPI/meta summary chips from the contacts list page. Keep the
existing search, status/visibility filters, tag filter, create action, table,
and drawer behavior.

Acceptance criteria:

1. `/contacts` no longer renders the top total/active/public/confidential
   summary chip row.
2. Existing search, filters, tag filtering, create action, and table remain.
3. Focused frontend verification passes for the changed route file.

## ActiveForm

Removing top stats from the contacts list.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

2026-05-25 18:30 - Investigation found the top summary row is rendered by
`SummaryChip` in `contacts/index.lazy.tsx`. The `kpis` object is still needed
for filter chip counts, so only the summary row and unused component should be
removed.

2026-05-25 18:35 - Removed the contacts summary row and unused `SummaryChip`
component, then verified the changed route with ESLint plus web typecheck.
