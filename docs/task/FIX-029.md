# FIX-029 Issues tab: tag filter to front + remove "按标签筛选" label

- Status: Completed
- Plan: [PLAN-052](../plan/PLAN-052.md)
- Owner: BKD L3 (campaign l1-75ymcfnr-tagfit-20260602054625)
- Campaign: l1-75ymcfnr-tagfit-20260602054625
- Updated: 2026-06-02

## Goal

In the project issues tab toolbar/filter row, put the tag filter FIRST and
remove the visible "按标签筛选" / "Filter by tag" label that precedes it.

File: `apps/web/src/app/routes/_app/projects/-project-issues-tab.tsx`

1. Ensure `<ProjectTagFilter>` is FIRST in the filter row (before the search
   box / create button group). It currently sits on the left of a
   `justify-between` row already — keep it first after the change.
2. Remove the visible label `<span>` (the `<Tags/>` icon + `{t("issues.tagFilter")}`
   text, ~lines 320-323) that precedes `<ProjectTagFilter>`.
3. The surrounding `role="group" aria-label={t("issues.tagFilter")}` MAY keep
   using the key for accessibility (non-visible). Grep `issues.tagFilter`
   usages: if the key is still referenced (e.g. by the group aria-label) keep
   it; only if NO consumer references it after the edit, drop it from en+zh
   (`locales/{en,zh}/projects.json`) keeping parity.

Layout + label removal only — tag filtering stays fully functional. If the
`Tags` icon import becomes unused after removing the span, remove that import.

## Scope

- `apps/web/src/app/routes/_app/projects/-project-issues-tab.tsx`
- `apps/web/src/locales/{en,zh}/projects.json` (only if the key becomes unused)

Out of scope: `-project-tag-filter.tsx` (responsive work = FIX-028, different
L3), the home row (FIX-030), procurement tab, backend.

## Acceptance

- Tag filter renders first in the issues filter row; no visible "按标签筛选"
  label remains.
- Tag multi-select filtering still works (chips toggle, search/create intact).
- `issues.tagFilter` key handled per grep: kept if still referenced, else
  removed from en+zh with parity. No unused imports left by the edit.
- `bun run check` exits 0 (`@milkdown/ctx` flake in
  `-project-issue-panel.test.tsx` is KNOWN — grep to confirm; `bun install`
  first if fresh worktree errors 127).

## Notes

- 2026-06-02 - Created at L2 xgbm1bkf (scope addition, PLAN-052).
- 2026-06-02 - L3 e0xamx5y green (commit 163b74b): removed visible label span (Tags icon + tagFilter text), ProjectTagFilter now first inside the group; `issues.tagFilter` kept (still used by group aria-label) → no json edit; unused Tags import dropped. Merged --no-ff into bkd/xgbm1bkf. Completed.
