# FIX-021 Tag-filter selector redesign

- Status: Completed
- Plan: [PLAN-047](../plan/PLAN-047.md)
- Owner: BKD L3 54sx59k7
- Campaign: l1-75ymcfnr-uipolish-20260602015825
- Updated: 2026-06-02

## Goal

Redesign `ProjectTagFilter` from inline-fit preset chips + "More" overflow into
a single tag SELECTOR control listing ALL tags with checkable state, plus the
SELECTED tags rendered as removable chips (X to deselect) to the right.

## Scope

- `apps/web/src/app/routes/_app/projects/-project-tag-filter.tsx`
- `apps/web/src/app/routes/_app/projects/-project-tag-filter-logic.ts` (remove
  the now-unused `computeVisibleTagCount`; delete the file if it becomes empty)
- `apps/web/src/app/routes/_app/projects/-project-tag-filter.test.tsx`

Out of scope: the 3 consumers (issues tab, procurement tab, projects list
`index.lazy.tsx`) — their props must stay valid with NO edits.

## Acceptance

- Single selector lists ALL tags with a checkable state (combobox multi /
  dropdown single); selected tags appear as removable chips (X) to the right.
- Single-select shows one removable chip.
- Inline-fit logic removed: `MAX_INLINE_CHIPS`, `computeVisibleTagCount`,
  `ResizeObserver`, the hidden measuring layer.
- Public props/signatures UNCHANGED (`tags`, `multiple`, `selectedTagIds`,
  `onToggle`, `selectedTagId`, `onSelect`); all 3 consumers compile untouched.
- `-project-tag-filter.test.tsx` updated to the new UI and green.
- `bun run check` exits 0 (mind the known `@milkdown/ctx` flake).

## Notes

- 2026-06-02 - Created + dispatched by L2 vwfwi45t.
- 2026-06-02 - L3 54sx59k7 green. Single-select onSelect is set-only (not a
  toggle), so the single selected tag renders as a non-removable highlighted
  chip; multi-select uses a searchable Combobox + removable chips. Reused
  existing i18n keys only. Out-of-scope but unavoidable: deleted
  -project-tag-filter-logic.ts + its test (held only the removed
  computeVisibleTagCount), and adapted consumer test -project-issues-tab.test.tsx
  (consumer SOURCE untouched). L3 forgot to commit; L2 committed (2bf48de) then
  merged --no-ff into bkd/vwfwi45t. Post-merge integration `bun run check`
  REAL_EXIT=0 (A's source + B's adapted test verified together). Completed.
