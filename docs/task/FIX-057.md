# FIX-057 - Data-layer hardening batch (FK indexes, tuple onDelete, drive timestamps, PRAGMA optimize, payroll tx)

- Status: Completed
- Plan: [PLAN-104](../plan/PLAN-104.md)
- Campaign: `l1-bithk-arch-20260702084717`
- Owner: L3
- Created: 2026-07-02

## Summary

Small data-layer refinements from AUDIT-20260702-architecture.md → D13. Migrations MUST be
generated by drizzle-kit from schema changes (never hand-authored).

## Acceptance Criteria

- Indexes added on `files.uploaded_by`, `file_references.created_by`, `drive_entries.created_by`.
- `relation_tuples.created_by` gets explicit `onDelete: "set null"` (column already nullable).
- `drive_entries.created_at/updated_at` switch to ISO-8601 `$defaultFn` like every other table;
  migration backfills existing rows (`strftime` rewrite) so formats are uniform.
- `PRAGMA optimize` scheduled (hourly timer or the existing lode checkpoint hook — pick the
  simplest; document the choice in the code).
- `generatePayrollForPeriod` insert loop wrapped in one tx, keeping `onConflictDoNothing`
  idempotency.
- Migration replay test (`db/migrations.test.ts`) passes; `bun run seed` works on a fresh DB.
- `bun run check` passes.

## Files in Scope

- `apps/api/src/modules/{file,drive,policy}/schema.ts`, `apps/api/drizzle/` (generated),
  `apps/api/src/db/index.ts` or `apps/api/src/index.ts` (optimize hook),
  `apps/api/src/modules/hr/hr.payroll.service.ts`.

## Status Notes

- 2026-07-02: Completed — FK indexes added, `relation_tuples.created_by` set-null on delete,
  drive timestamps normalized to ISO-8601 with backfill, `PRAGMA optimize` scheduled, payroll
  generation wrapped in one transaction.
