# FIX-010 Data-integrity / transaction correctness (Campaign C)

- Status: Completed
- Plan: [PLAN-044](../plan/PLAN-044.md)
- Campaign: l1-75ymcfnr-projaudit-20260601230043
- Updated: 2026-06-01

## Goal

Make multi-row cleanups atomic and pin down the delete-cascade contract;
document the chosen soft/hard-delete semantics in a decision record.

## Findings

| ID | Sev | Title | Location |
| --- | --- | --- | --- |
| 04-F1 | P1 | Contact hard-delete cleans `tags_refs` outside any transaction | `contact.service.ts:218-233` |
| 04-F2 | P2 | Project soft-delete does not cascade; child rows stay live forever | `project.service.ts:429-438` |
| 04-F3 | P2 | Project/item hard-delete cascade bypasses `tags_refs` cleanup | `project/schema.ts:48`; `item/schema.ts:20` |
| 04-F4 | P3 | Cover-image reference release not atomic with project update | `project.service.ts:466-500,534-566` |
| 04-F6 | P3 | `deleteRole` "guest missing" fallback would FK-fail, not degrade | `project.roles.ts:209-228` |
| 04-F7 | P3 | `createProject` default-cover existence check is TOCTOU vs insert FK | `project.service.ts:281-307` |
| 04-F5 | P3 | (optional) project update bumps version w/o optimistic-concurrency check | `project.service.ts:403-422` |

## Acceptance

- Contact hard-delete (row + `tags_refs` + tuples + shares) runs in ONE
  transaction; test covers rollback-on-failure.
- Soft-delete cascade semantics (04-F2) and hard-delete `tags_refs` cascade
  contract (04-F3) chosen explicitly and recorded in `docs/decisions/008-*.md`
  (next free number after 007) with a dev-phase / sunset note.
- Cover-ref release atomic with project update; `deleteRole` fallback degrades
  cleanly (no FK fail); default-cover check no longer TOCTOU.
- `bun run check` EXIT=0.

## Scope

- In: `apps/api/src/modules/{contact/contact.service.ts,project/project.service.ts,project/project.roles.ts,project/schema.ts,item/schema.ts}` + targeted tests + `docs/decisions/008-*.md`.
- Out: role-authz logic (Campaign A), performance count fns (Campaign B).

## Dependencies

After A (shared `project.service.ts` / `project.roles.ts`).

## Notes

- 2026-06-01 23:2X — Dispatched as BKD L3 `b7bikss6` (status working). Deps[A] satisfied (A merged 171619a). Constrained to data-integrity functions only (A owns the authz functions in project.service.ts/project.roles.ts) to avoid merge conflict. Decision doc target: docs/decisions/008.

## Verification (2026-06-01 — L3 b7bikss6, cherry-picked dafb7df)

`bun run check` EXIT=0; API 1217 pass (+5), 0 fail. Drizzle migration
`0001_hot_whistler` added for the tags_refs cascade contract (forks the same
0000 baseline → no collision). decision doc `docs/decisions/008-delete-cascade-semantics.md` created.

- 04-F1 contact hard-delete cleanup wrapped in one tx (+ test).
- 04-F2 soft-delete cascade semantics + 04-F3 hard-delete tags_refs cascade
  contract decided & recorded in decision 008; schema (project + item) + migration 0001.
- 04-F4 cover-ref release atomic; 04-F6 deleteRole guest fallback resolves safely;
  04-F7 createProject default-cover TOCTOU removed.
- MERGE NOTE: b7bikss6 branch carried the same 2 stray base commits
  (Chinese ui-consistency doc + .gitignore chore) as the G2 branch — L2
  cherry-picked ONLY the in-scope work commit (23008f5 → dafb7df), dropping the
  strays. git auto-merged A's + C's disjoint `project.service.ts` regions.

## Deferral note (2026-06-01 — from C report)

Substantive findings resolved (04-F1 tx, 04-F2 soft-delete cascade, 04-F3
hard-delete RESTRICT contract, 04-F6 deleteRole guard, 04-F7 TOCTOU). Two P3s
deferred with documented mitigation in decision 008:
- 04-F4 (cover-ref atomicity) — true atomicity needs a tx-aware reference-release
  API in `file.service.ts` (outside the C lane); current repoint-before-release
  ordering bounds the leak. Escalated to L1.
- 04-F5 (optimistic concurrency) — only useful with a route-layer `expectedVersion`
  (`project.routes.ts`, out of scope); low risk. Escalated to L1.
