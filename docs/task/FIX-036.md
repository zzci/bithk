# FIX-036 Issues tab tag-filter label "按标签筛选" -> "标签"

- Status: Implementing
- Plan: [PLAN-062](../plan/PLAN-062.md)
- Owner: BKD L3 (campaign l1-75ymcfnr-isstag-20260603191110)
- Campaign: l1-75ymcfnr-isstag-20260603191110
- Updated: 2026-06-03

## Goal

Change the project issues (工单) tab tag-filter dimension label from
"按标签筛选" / "Filter by tag" to "标签" / "Tags". The label is the
`issues.tagFilter` i18n value consumed by `<ListFilter>` at
`apps/web/src/app/routes/_app/projects/-project-issues-tab.tsx:325`.

1. `apps/web/src/locales/zh/projects.json` — `issues.tagFilter` "按标签筛选" -> "标签".
2. `apps/web/src/locales/en/projects.json` — `issues.tagFilter` "Filter by tag" -> "Tags".
3. `apps/web/src/app/routes/_app/projects/-project-issues-tab.test.tsx` — update
   the assertion `findByRole("button", { name: "Filter by tag" })` (line ~172)
   to `name: "Tags"`.

i18n-value + test only. No component logic change (the component already reads
the key).

## Scope

- `apps/web/src/locales/zh/projects.json` (issues.tagFilter value only)
- `apps/web/src/locales/en/projects.json` (issues.tagFilter value only)
- `apps/web/src/app/routes/_app/projects/-project-issues-tab.test.tsx`

Out of scope: `procurement.tagFilter` (line 192, separate key), any
`tagFilterMore*` keys, `-project-issues-tab.tsx` logic, `-project-tag-filter.tsx`,
ships locales, procurement tab, backend.

## Acceptance

- `issues.tagFilter` = "标签" (zh) / "Tags" (en); `procurement.tagFilter`
  untouched.
- en/zh parity preserved (only the two values changed; key set identical).
- Issues-tab test asserts the "Tags" label and passes.
- `bun run check` exits 0 (`@milkdown/ctx` flake in
  `-project-issue-panel.test.tsx` is KNOWN; `bun install` first if fresh
  worktree errors 127).

## Notes

- 2026-06-03 - Created at L2 v7tozyh2 (PLAN-062, campaign l1-75ymcfnr-isstag-20260603191110).
