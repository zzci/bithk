# FIX-007 Restore project list description and tag spacing

- Status: Completed
- Plan: [PLAN-029](../plan/PLAN-029.md)
- Owner: BKD L3 ucxzqk1i
- Updated: 2026-05-28

## Goal

Restore the `/projects` list card body so project descriptions are visible
again and the bottom tag row reserves stable space even for projects without
tags.

Campaign: `l1-cuu89zau-20260528222811`

## Scope

- `apps/web/src/app/routes/_app/projects/index.lazy.tsx`
- Focused project list tests near the route/component.
- Direct locale text only if an existing key is insufficient.

Out of scope: backend/API/schema changes unless the current API does not expose
project descriptions, project detail tabs, work-order UI, procurement UI, global
tag schema changes, and unrelated visual refactors.

## Acceptance

- Project cards with descriptions display those descriptions clearly in the
  list/card body, in a dedicated row of each card/list item.
- The tag area occupies a separate dedicated row below the description.
- Project cards without tags still reserve the same bottom tag-row space as
  cards with tags, keeping heights and action alignment stable.
- Description and tags are not merged into the same line or metadata cluster.
- Existing tag chip rendering and tag filter behavior remain intact.
- The layout remains responsive and aligned with the current shadcn/frontdesign
  style used by recent project pages.
- Focused tests cover description rendering and tag-placeholder spacing where
  practical.
- Focused web tests and `bun run check` are run before marking green.

## Notes

- 2026-05-28 22:30 - L2 investigation started for campaign
  `l1-cuu89zau-20260528222811`. The frontend project API type and list search
  already include `description`, so no backend contract change is indicated.
- 2026-05-28 22:35 - Current regression source: `ProjectsGrid` renders only
  cover, title/status/settings, and conditional tags. Description is not
  rendered, and `CardContent` for tags is omitted when `project.tags.length` is
  zero.
- 2026-05-28 22:40 - L1 supplemental requirement received for campaign
  `l1-cuu89zau-20260528222811`: description must have its own row, tags must
  have a separate row below it, and the tag row must reserve vertical space
  even when empty.
- 2026-05-28 22:45 - BKD L3 ucxzqk1i implemented the fix in `ProjectsGrid`
  (`index.lazy.tsx`): `CardContent` now always renders as a `flex flex-col
  gap-3`; a `line-clamp-2` description paragraph renders only when
  `description.trim()` is non-empty; the tag chips moved to a dedicated row
  below it that always renders with `min-h-5` (matching Badge height) so
  tagless cards reserve the same bottom space. Tag slice(0,3)/overflow `+N`
  and tag-filter behavior unchanged; no backend/locale changes. Exported
  `ProjectsListPage` for the focused test. Added
  `-project-list.test.tsx` (4 cases: description shown, whitespace/null
  description omitted, tagless placeholder row reserved, tagged chip/overflow
  intact). Focused tests pass.
- 2026-05-28 22:50 - L2 supplemental row-separation requirement confirmed met
  by the existing flex-col layout (description `<p>` row + separate `min-h-5`
  tag row). Strengthened the focused test with a fifth case asserting the
  description and tag rows are distinct sibling elements under a `flex-col`
  container and neither nests the other, so merging them into one
  row/metadata cluster would fail the test. 5/5 focused tests pass.
- 2026-05-28 23:00 - L2 quality assessment completed for campaign
  `l1-cuu89zau-20260528222811`. L3 `ucxzqk1i` is green, focused tests pass
  5/5, `bun run check` exits 0, and the issue was left in BKD `review`.
  Simple-mode work is already integrated in the shared main worktree.
