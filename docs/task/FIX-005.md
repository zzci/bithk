# FIX-005 Open project settings in place from the list

- Status: Done
- Plan: -
- Updated: 2026-05-27

## Goal

Clicking the settings button on a project list card should open the settings
dialog in place on the list, not navigate into the project detail page.

## Scope

1. `index.lazy.tsx`: replace the `?settings=true` navigation with a
   `settingsProjectId` state. Lazily load that project's detail (with
   capabilities), members, and user names, and render `ProjectSettingsDialog`
   over the list.
2. The detail page `?settings=true` deep link is left untouched.

## Verification

- `bun run check` passes.
- Clicking a card's settings gear opens the dialog without leaving the list;
  closing it keeps the user on the list.
