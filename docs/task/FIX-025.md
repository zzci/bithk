# FIX-025 Remove projects-home filter-row label

- Status: Completed
- Plan: [PLAN-048](../plan/PLAN-048.md)
- Owner: BKD L3 qofnssso
- Campaign: l1-75ymcfnr-tagfx-20260602025356
- Updated: 2026-06-02

## Goal

Remove the leading "筛选:"/"Filter:" label from the projects-home unified filter
row. The row keeps the status chips (正常/已归档) + tag selector + removable
selected-tag chips, just with no leading label text.

## Scope

- `apps/web/src/app/routes/_app/projects/index.lazy.tsx` (remove the
  `<span>{t("list.filterLabel")}</span>` around line 107)
- `apps/web/src/locales/{en,zh}/projects.json` (remove the now-unused
  `list.filterLabel` key; keep en/zh parity)

Out of scope: `-project-tag-filter.tsx` (sibling L3 FIX-024 owns it). No other
behavior or layout restructure.

## Acceptance

- Label span gone; row starts directly with the status chips.
- `filterLabel` removed from both locales; en/zh parity intact; no dangling refs.
- Row spacing/alignment still correct.
- `bun run check` exits 0 (mind the known `@milkdown/ctx` flake).

## Notes

- 2026-06-02 - Created + dispatched by L2 h9ieukwl to L3 qofnssso (working);
  parallel to FIX-024 (disjoint file, non-adjacent JSON keys).
- 2026-06-02 - L3 qofnssso green (commit 20c61cc): label span removed,
  `filterLabel` key dropped from both locales, parity intact. Merged --no-ff
  into bkd/h9ieukwl (2aed21a); `bun run check` EXIT=0. Completed.
