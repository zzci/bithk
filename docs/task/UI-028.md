# UI-028 - Web data-layer discipline: query keys, http() call sites, keepPreviousData

- Status: Completed
- Plan: [PLAN-104](../plan/PLAN-104.md)
- Campaign: `l1-bithk-arch-20260702084717`
- Owner: L3
- Created: 2026-07-02

## Summary

Query-key knowledge is duplicated (factory + inline literals), 36 route files call `http()`
directly bypassing the api layer, and only `ships.ts:180` uses `keepPreviousData`. See
AUDIT-20260702-architecture.md → D10.

## Acceptance Criteria

- No inline literal query keys in route files: `-project-issue-hooks.ts:57,71-72` and ad-hoc keys
  (`admin/-policies-shared.ts:54`, `-settings-about.tsx:95`) move into the owning
  `shared/lib/api/*` key factories.
- Direct `http()`/`httpRaw` calls in `src/app/routes/**` migrated into `shared/lib/api/*` hooks
  (typed, with invalidation); grep for `from "@/shared/lib/http"` under routes returns only
  justified exceptions (documented in code).
- `placeholderData: keepPreviousData` added to paginated list queries (projects, contacts, audit,
  groups, and siblings).
- Existing api-layer tests extended for moved hooks; no UX behavior change beyond eliminated
  blank-flash.
- `bun run check` passes (web coverage thresholds hold).

## Files in Scope

- `apps/web/src/shared/lib/api/*`, `apps/web/src/app/routes/**` (mechanical call-site moves).

## Status Notes

- 2026-07-02: Completed — inline query keys and direct `http()` call sites moved into the shared
  api layer's typed hooks; `keepPreviousData` added to paginated list queries.
