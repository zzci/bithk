# FIX-054 - Policy middleware: close silent fail-open branch + direct tests

- Status: In Progress
- Plan: [PLAN-104](../plan/PLAN-104.md)
- Campaign: `l1-bithk-arch-20260702084717`
- Owner: L3
- Created: 2026-07-02

## Summary

`modules/policy/middleware.ts` (the global route-authorization gate, 240 lines) has no test that
imports it, and line 203 (`if (!access || !def) return next();`) silently passes an unauthorized
request when a matched binding lacks a registered access instance — contradicting the fail-closed
boot assert (`app.ts:163-169`). See AUDIT-20260702-architecture.md → D3.

## Acceptance Criteria

- A matched route binding without a registered access instance/definition throws (500 wiring
  error), never falls through to `next()`.
- New `middleware.test.ts` covering: undeclared route passes through; declared route + actor
  without permission → 403/404 per current semantics; admin short-circuit; PAT-scope interaction
  unaffected; the new fail-closed branch.
- No change to legitimate request flows (`defineResource` registers both atomically today).
- `bun run check` passes.

## Files in Scope

- `apps/api/src/modules/policy/middleware.ts`, new `apps/api/src/modules/policy/middleware.test.ts`
