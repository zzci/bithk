# FIX-022 Procurement filter all-labels rename

- Status: Completed
- Plan: [PLAN-047](../plan/PLAN-047.md)
- Owner: BKD L3 zk4x1ss2
- Campaign: l1-75ymcfnr-uipolish-20260602015825
- Updated: 2026-06-02

## Goal

Shorten the three procurement ToolbarFilter "all" labels.

## Scope

- `apps/web/src/locales/en/projects.json` and `.../zh/projects.json`:
  - `procurement.allStatuses`: All statuses → Status / 全部状态 → 状态
  - `procurement.allPriorities`: All priorities → Priority / 全部优先级 → 优先级
  - `procurement.allCategories`: All categories → Category / 全部分类 → 分类
- `apps/web/src/app/routes/_app/projects/-project-procurement-tab.tsx` — no logic
  change expected; the ToolbarFilter already reads these keys via `allLabel`.

Out of scope: any other keys; status/priority/category enums; backend.

## Dependencies

- deps=[FIX-020] — both edit `projects.json`; serialized to avoid a merge
  conflict. Dispatched after FIX-020 merges.

## Acceptance

- The three procurement filter triggers show the shortened labels in en + zh.
- i18n en/zh parity guard stays green.
- `bun run check` exits 0 (mind the known `@milkdown/ctx` flake).

## Notes

- 2026-06-02 - Created by L2 vwfwi45t; held (deps=FIX-020).
- 2026-06-02 - L3 zk4x1ss2 green. Edited only the canonical toolbar trio
  (~224-226) in both locales; left the pre-existing duplicate allPriorities at
  line 161 and issues.* untouched; procurement-tab.tsx needed no change. Also
  updated -project-procurement-tab.test.tsx assertions (getByRole button to
  avoid the 'Status' table-header collision). L3 committed (dfa4738); L2 merged
  --no-ff into bkd/vwfwi45t. Post-merge `bun run check` REAL_EXIT=0. Completed.
