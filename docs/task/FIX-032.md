# FIX-032 Projects home: tag filter to the RIGHT of status chips

- Status: Planned
- Plan: [PLAN-053](../plan/PLAN-053.md)
- Owner: BKD L3 (campaign l1-75ymcfnr-tagpos-20260602063716)
- Campaign: l1-75ymcfnr-tagpos-20260602063716
- Updated: 2026-06-02

## Problem (revised 2026-06-02 — supersedes the original pure-reorder)

On the projects-home filter row, the filter CONTROLS (标签 selector trigger +
pinned common-tag chips + `正常/已归档` status chips) must stay on the LEFT. Only
the newly-SELECTED removable × chips (those that appear after picking a
non-pinned tag) should be pushed to the RIGHT (right-aligned), separated from the
left controls.

Desired row: LEFT = [标签 selector] [pinned common tags] [正常] [已归档]  ……  RIGHT = [selected removable × chips].

## Dependency

Depends on FIX-031 (same file `-project-tag-filter.tsx`). Serialized to avoid a
merge conflict — dispatched only after FIX-031 merges into `bkd/p57t0zqd`, and
the L3 must incorporate FIX-031 first (`git merge bkd/p57t0zqd`).

## Scope

- `apps/web/src/app/routes/_app/projects/-project-tag-filter.tsx` (add an
  optional, home-only prop so the selected removable chips render in a
  right-aligned slot while pinned+selector stay left; issues/procurement paths
  must stay visually identical → prop-gated, default unchanged)
- `apps/web/src/app/routes/_app/projects/index.lazy.tsx` (filter row ~105-153:
  inject status chips into the left subgroup; let selected chips sit right)

## Requirement

- Left subgroup: pinned chips + 标签 selector + 正常/已归档 status chips.
- Right subgroup: selected removable × chips, right-aligned (e.g. justify-between
  / `ml-auto`).
- HOME-specific. Issues-tab and procurement layout MUST remain unchanged
  (achieved by gating the new behavior behind an optional prop that those
  consumers do not pass).
- Does NOT change FIX-031 pinned-chip 常驻 behavior.

## Acceptance

- Projects-home row: selector+pinned+status on the left, selected × chips on the
  right.
- Issues-tab / procurement layout unchanged.
- `bun run check` green (web + api), no new failures.
