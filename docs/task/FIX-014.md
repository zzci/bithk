# FIX-014 i18n parity + CI guard (Campaign G)

- Status: In Progress
- Plan: [PLAN-044](../plan/PLAN-044.md)
- Campaign: l1-75ymcfnr-projaudit-20260601230043
- Updated: 2026-06-01

## Goal

Translate the untranslated `zh/drive.json` values and add an automated en/zh
parity guard to `bun run check` so this class of regression is caught.

## Findings

| ID | Sev | Title | Location |
| --- | --- | --- | --- |
| 06-F1 | P2 | `zh/drive.json` has 7 untranslated (English) values | `locales/zh/drive.json:30,99,203-207` |
| 06-F4 | P2 | No automated en/zh key-parity / untranslated-value guard | `apps/web/src/app/i18n.ts` |
| 06-F2 | P3 | English plural forms not handled for `{{count}}` interpolations | `locales/en/projects.json:37,165,221,207-208`; `drive.json:48` |
| 06-F3 | P3 | `projects` ns duplicates `issues.status` / `issues.group` verbatim | `locales/en/projects.json:184-197` |

## Acceptance

- The 7 English `zh/drive.json` values translated to Chinese.
- A Vitest spec asserts en/zh key-set parity + warns on ASCII-identical values,
  wired into `bun run check` so it runs in the gate.
- English plural forms added; duplicated status/group blocks de-duplicated.
- `bun run check` EXIT=0 (and the new parity spec passes).

## Scope

- In: `apps/web/src/app/locales/{en,zh}/{drive.json,projects.json}`,
  `apps/web/src/app/i18n.ts`, a new Vitest parity spec + its wiring into the
  check command.
- Out: all backend + other-namespace logic.

## Dependencies

Independent (disjoint files).

## Notes

- 2026-06-01 23:0X — Dispatched as BKD L3 `j7km0cv8` (status working).

## Verification (2026-06-01 — L3 j7km0cv8, merged into e742312)

`bun run check` EXIT=0 (web 565 pass incl. new parity spec, 22 cases). L3
commit-gap: L2 committed (dd20c00) before merge. NOTE real locale path is
`apps/web/src/locales/*` (not `…/app/locales/*`).

- 06-F1 (P2) DONE — 7 zh/drive.json values translated (uploadFolder, versions.*).
- 06-F4 (P2) DONE — new `src/locales/i18n-parity.test.ts`: HARD-fails on en/zh
  key-set divergence + on NEW ASCII-identical leaf values (curated allowlist for
  the 6 intentional acronym/placeholder entries). Runs under `bun run check`.
- 06-F2 (P3) DEFERRED — needs `zh/projects.json` + `scripts/check-i18n.ts` +
  `scripts/lib/i18n-scan.ts` (out of this campaign's scope); plural keys are
  en/zh-asymmetric and would trip the existing check-i18n gate. Audit rates it
  "cosmetic / low priority for dev phase".
- 06-F3 (P3) DEFERRED — needs component lookup redirects (group→status) +
  removing the dup block from both locales (out of scope); en-only removal would
  break key parity. Audit: "defer unless duplication is actually causing drift".

**Status: P2s done; F2/F3 (P3) deferred — escalated to L1 for accept-vs-followup.**
