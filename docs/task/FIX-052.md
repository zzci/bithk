# FIX-052 - P3 hardening bundle (P3)

- Status: Completed
- Plan: [PLAN-100](../plan/PLAN-100.md)
- Campaign: `l1-bithk-audit-20260701145456`
- Owner: L3
- Created: 2026-07-01

## Summary

Five small, independent P3 hardening items bundled into one task. See AUDIT-20260701.md → P3.

## Acceptance Criteria

Each item done; `bun run check` passes.

1. **service-token constant-time compare** — `apps/api/src/shared/middleware/service-token.ts:35`: remove the
   `a.length !== b.length` early return; hash both sides to a fixed width (e.g. SHA-256) before
   `timingSafeEqual`, matching the PAT comparison path. Keep behavior identical for valid/invalid tokens.
2. **stop backup-staging sweep on shutdown** — `apps/api/src/index.ts` `closeServices`: add
   `await safe("stopBackupStagingSweep", stopBackupStagingSweep, silent);` alongside the existing audit/
   file-GC/cron stops. `stopBackupStagingSweep` is already exported from `apps/api/src/modules/backup`.
3. **file-GC re-entrancy guard** — `apps/api/src/modules/file/gc.ts:51-82`: add an in-flight boolean so an
   overlapping tick is skipped, mirroring `apps/api/src/modules/audit/retention.ts`. Keep the existing
   try/catch (a thrown DB error must not kill the interval).
4. **client redirect scheme checks** — `apps/web/src/app/routes/denied.tsx:108` (logout-url) and
   `apps/web/src/app/routes/totp-verify.tsx:38` (redirect): validate the URL is `https://…` or a `/`-relative
   path (reuse the `isSafeRedirect` helper used by `login.tsx:36`) before assigning to `href`/`location`.
5. **remove dead `DB_ENCRYPTION` env** — delete the `DB_ENCRYPTION=false` line from `.env.docker` (not read
   anywhere; zod strips it silently, giving a false sense of an encryption knob). Do NOT add it to the schema.

## Files in Scope

- `apps/api/src/shared/middleware/service-token.ts`
- `apps/api/src/index.ts`
- `apps/api/src/modules/file/gc.ts`
- `apps/web/src/app/routes/denied.tsx`, `apps/web/src/app/routes/totp-verify.tsx`
- `.env.docker`
- Co-located tests as needed (`service-token.test.ts`, `gc` test if present)

## Design constraints

- Each item is independent; a blocker on one must not block the others (report per-item status).
- No new env knobs; no behavior change beyond the stated hardening.
- Items 4 (web) and 1-3,5 (api/config) touch disjoint files — safe to implement together.
