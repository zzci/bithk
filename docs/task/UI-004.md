# UI-004 Prototype parity remediation for ships, projects, and contacts

- **status**: completed
- **priority**: P0
- **owner**: BKD campaign l1-mwo9qmid-20260525155159
- **createdAt**: 2026-05-25 17:10

## Description

Human verification rejected UI-001. The implemented ships, projects, and
contacts pages differ materially from the reference prototype at
`https://fr.ds.cc/bit.html`, and the projects page has a blocking runtime error:
`Invalid language tag: en-US@posix`.

Acceptance criteria:

1. `/projects` renders in a real browser without the locale-tag runtime error.
2. Ships, projects, and contacts content pages are visually and functionally
   aligned with the reference prototype within the original scope: content pages
   only, no global sidebar or app shell rewrite unless explicitly approved.
3. Missing content-page controls and density from the prototype are restored or
   explicitly documented as impossible due to missing backend/domain support.
4. A real-browser smoke check covers `/ships`, `/projects`, and `/contacts`.
5. Final verification includes direct comparison against
   `https://fr.ds.cc/bit.html` screenshots.

## ActiveForm

Coordinating BKD L2/L3 remediation work after failed human verification.

## Dependencies

- **blocked by**: UI-001 human verification failure
- **blocks**: UI-001 acceptance

## Notes

2026-05-25 17:10 - Human directed L1 to resume coordination and have L2
decompose L3 remediation tasks. L1 will coordinate only; implementation belongs
to L2/L3.

2026-05-25 18:30 - Remediation completed on main at `0f2c359`. Final gates:
`bun run check` passed and `bun run smoke` passed 3/3 under `en-US@posix`.
Prototype comparison artifacts and residual backend-gated gaps are documented in
`docs/plan/PLAN-019-parity-closeout.md`.
