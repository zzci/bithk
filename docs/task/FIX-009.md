# FIX-009 Issue-list / project-list performance (Campaign B)

- Status: Planned
- Plan: [PLAN-044](../plan/PLAN-044.md)
- Campaign: l1-75ymcfnr-projaudit-20260601230043
- Updated: 2026-06-01

## Goal

Port the proven procurement JOIN pattern (`procurement.service.ts:410-468`) to
the issue path, collapse the 5-request status fan-out, and stop fetching full
pages just for counts.

## Findings

| ID | Sev | Title | Location |
| --- | --- | --- | --- |
| 03-F1 | P1 | `composeIssue` runs 3 queries per row (N+1) | `issue.service.ts:412-414,94-121` |
| 03-F2 | P1 | Issues tab fans out 5 full list requests (one per status) | `-project-issues-tab.tsx:256-260` |
| 03-F3 | P1 | `listByProject` two-phase unbounded `inArray` vs a JOIN | `issue.service.ts:381-410` |
| 03-F4 | P1 | Project list fetches two extra full pages just for status counts | `index.lazy.tsx:50-51`; `project.service.ts:347-394` |
| 03-F5 | P2 | `searchIssues` is N+1 with no batching | `issue.service.ts:458-462` |
| 03-F6 | P2 | No standalone index on `project_members.user_id` (new migration) | `project/schema.ts:89-95` |
| 03-F7 | P2 | `loadResourceTagsByResource` correlated COUNT per (row,tag) | `tag.service.ts:235-245` |
| 03-F8 | P2 | Overview + tab bar issue redundant count vs latest queries | `$projectId.lazy.tsx:54-55`; `-project-overview-tab.tsx:40-41` |
| 03-F11 | P3 | Large tabs not memoized; inline callbacks recreate each render | `-project-issues-tab.tsx`; `-project-procurement-tab.tsx` |

## Acceptance

- Issue list path uses a JOIN (`items ⋈ issueDetails`), resolves the project
  short id once per page, batches assignee tuples + tags; no per-row N+1.
- Issues tab issues ONE list request grouped client-side, not 5.
- Project-list count queries use `limit:1` (or a counts endpoint), not full
  pages.
- `searchIssues` batched; `loadResourceTagsByResource` no correlated subquery.
- Migration adds `project_members.user_id` index.
- Tabs memoized where cheap.
- `bun run check` EXIT=0.

## Scope

- In: `apps/api/src/modules/issue/issue.service.ts`, `tag/tag.service.ts`,
  `project/{schema.ts,project.service.ts}` (count fns only — coordinate ranges
  with A/C), a new Drizzle migration, and
  `apps/web/.../projects/{index.lazy.tsx,$projectId.lazy.tsx,-project-issues-tab.tsx,-project-procurement-tab.tsx,-project-overview-tab.tsx}`.
- Out: search-correctness UX (Campaign F).

## Dependencies

After A and C (shared `project.service.ts`).

## Notes
