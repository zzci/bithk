# FIX-011 Detail-drawer accessibility (Campaign D)

- Status: Planned
- Plan: [PLAN-044](../plan/PLAN-044.md)
- Campaign: l1-75ymcfnr-projaudit-20260601230043
- Updated: 2026-06-01

## Goal

Detail drawers (issue + procurement) get a proper focus trap, focus
restoration, accessible name, and keyboard-operable resize. **SUSPECTED already
fixed** by the prior uifix campaign (merges b3c1693 / 66ea954 / b85fe84 +
`-resizable-drawer.tsx`). RE-AUDIT current source against each finding's
`file:line` FIRST, cite evidence, fix only residual gaps.

## Findings

| ID | Sev | Title | Location |
| --- | --- | --- | --- |
| 05-F1 | P1 | Detail drawers hand-rolled modals: no focus trap / restoration | `$projectId.issues.$issueId.lazy.tsx:96-137`; `$projectId.procurements.$procurementId.lazy.tsx:96-139` |
| 05-F2 | P2 | Issue drawer `role="dialog"` has no accessible name | `$projectId.issues.$issueId.lazy.tsx:102-107` |
| 05-F15 | P3 | Drawer resize handle has no keyboard support | `$projectId.issues.$issueId.lazy.tsx:111-119`; procurements `:112-120` |

## Acceptance

- For EACH finding: current `file:line` evidence recorded here (resolved vs
  residual). Residual gaps fixed via the shared drawer/Sheet primitive (focus
  trap, restoration, `inert`, Escape, scroll-lock, accessible name, keyboard
  resize).
- `bun run check` EXIT=0.

## Scope

- In: `apps/web/.../projects/$projectId.issues.$issueId.lazy.tsx`,
  `$projectId.procurements.$procurementId.lazy.tsx`,
  `-resizable-drawer.tsx` (+ shared Sheet primitive) + targeted tests.
- Out: panel-body button sweep (Campaign E).

## Notes
