# FIX-023 Projects home filter row unification

- Status: Planned
- Plan: [PLAN-047](../plan/PLAN-047.md)
- Owner: BKD L3 8nsvr6fd
- Campaign: l1-75ymcfnr-uipolish-20260602015825
- Updated: 2026-06-02

## Goal

Unify the projects-home filter bar into ONE consistent "筛选:" chip row where
the status filters (正常 / 已归档) and the tag selector + selected tags all share
the same chip/button visual language.

## Scope

- `apps/web/src/app/routes/_app/projects/index.lazy.tsx` (the filter bar,
  ~lines 105-150)
- `apps/web/src/locales/{en,zh}/projects.json` — only new keys (e.g. a `筛选:` /
  `Filter:` label), keep parity.

Consumes the redesigned `ProjectTagFilter` (selector + removable chips) from
FIX-021. Out of scope: editing `-project-tag-filter.tsx` (owned by FIX-021),
backend, list-query behavior.

## Dependencies

- deps=[FIX-021] — D renders B's redesigned ProjectTagFilter selector +
  removable-chip rendering. Dispatch only after FIX-021 merges into
  bkd/vwfwi45t. Disjoint file from A (issues-tab) and C (procurement-tab), so D
  may run parallel to A once B is merged.

## Acceptance

- One "筛选:" row: leading label, 正常/已归档 as tag-style chip buttons (count
  badges kept, toggle behavior unchanged), the tag selector, and removable
  selected tag chips — all one consistent chip visual (no old filled-vs-outline
  status buttons).
- Active/archived filtering + single-select tag filtering behave identically to
  today.
- en/zh parity for any new strings.
- `bun run check` exits 0 (mind the known `@milkdown/ctx` flake).

## Notes

- 2026-06-02 - Created by L2 vwfwi45t (scope addition D); held (deps=FIX-021).
