# FIX-015 Dead code + test gaps (Campaign H)

- Status: In Progress
- Plan: [PLAN-044](../plan/PLAN-044.md)
- Campaign: l1-75ymcfnr-projaudit-20260601230043
- Updated: 2026-06-01

## Goal

Resolve the tag-registry dead read-side, drop genuinely dead UI code, tighten
over-exports, and fill the named test gaps.

## Findings

| ID | Sev | Title | Location |
| --- | --- | --- | --- |
| 07-F1 | P2 | Tag registry read-side is dead code; module comment misleading | `tag.registry.ts:20,28,4-7` |
| 07-F2 | P2 | `projectBackupContribution` has no test (backup round-trip gap) | `project.backup.ts:4`; `project/index.ts:7` |
| 01-F2 | P3 | `StatCard` / `StatStrip` are dead code (defined, never rendered) | `-project-stats.tsx:1-69` |
| 01-F6 | P3 | Stale header/inline comments describe removed UI | `-project-issues-tab.tsx:1-10,71-72` |
| 07-F3 | P3 | Project detail tab-nav shell untested | `$projectId.lazy.tsx:37-190` |
| 07-F4 | P3 | Per-issue React-Query hooks untested | `-project-issue-hooks.ts:27-75` |
| 07-F5 | P3 | `tag.registry.ts` has no test | `tag.registry.ts` |
| 07-F6 | P3 | `assertValidTagName` over-exported | `tag.service.ts:30` |
| 07-F8 | P3 | `-project-cover-field.tsx` and `-project-stats.tsx` untested | `-project-cover-field.tsx`; `-project-stats.tsx:19-54` |
| 07-F9 | P3 | Two narrow branch gaps: category PATCH-404 + cover-hook admin bypass | `project.routes.ts:422-430`; `project.cover.permission.ts:16,22` |

## Acceptance

- Tag-registry dead read-side resolved (delete or wire `/tags` to it) +
  misleading comment fixed; coordinate with 07-F5.
- `-project-stats.tsx` dead `StatCard`/`StatStrip` removed (subsumes 01-F3) OR
  rendered — pick deletion if still unused; stale comments removed.
- `assertValidTagName` un-exported (or test added if a consumer exists).
- Tests added for backup round-trip, tab-nav shell, issue hooks, registry,
  cover-field, category PATCH-404, cover-hook admin bypass.
- `bun run check` EXIT=0.

## Scope

- In: `apps/api/src/modules/tag/{tag.registry.ts,tag.service.ts}`,
  `project/{project.backup.ts,project.routes.ts,project.cover.permission.ts}`,
  `apps/web/.../projects/{-project-stats.tsx,-project-issues-tab.tsx,-project-cover-field.tsx,-project-issue-hooks.ts,$projectId.lazy.tsx}` + tests.
- Out: nothing else (last campaign).

## Dependencies

After B, E, F (coordinates `tag.service.ts`, `-project-stats.tsx`,
`-project-issues-tab.tsx`).

## Notes

- 2026-06-02 00:3X — Dispatched as BKD L3 `u69p4lnr` (working). Deps[F] met. FINAL campaign. Told to prefer DELETING the tag-registry dead read-side and -project-stats.tsx, and to describe B's current single-query UI when fixing stale issues-tab comments.
