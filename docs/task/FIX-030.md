# FIX-030 Projects home: tag filter to front of filter row

- Status: In Progress
- Plan: [PLAN-051](../plan/PLAN-051.md)
- Owner: BKD L3 (campaign l1-75ymcfnr-tagfit-20260602054625)
- Campaign: l1-75ymcfnr-tagfit-20260602054625
- Updated: 2026-06-02

## Goal

In the projects-home unified filter row, move the tag filter (pinned chips +
标签 selector + selected chips) to the FRONT — before the 正常/已归档 status
chips. Mirrors the issues-tab "tag filter first" change.

File: `apps/web/src/app/routes/_app/projects/index.lazy.tsx`

The row (around line 104) currently maps the `__active__` / `__archived__`
status `Button`s first, then renders `<ProjectTagFilter>`. Reorder so
`<ProjectTagFilter>` comes FIRST, followed by the two status chips, inside the
same `flex` container.

Layout reorder only — keep all behavior: status toggles (single-select via
`filter` state), tag filtering (`onSelect`/`onClear`), pagination reset. The
"筛选:" label was already removed earlier — do NOT reintroduce it.

## Scope

- `apps/web/src/app/routes/_app/projects/index.lazy.tsx`

Out of scope: `-project-tag-filter.tsx` (FIX-028), the issues tab (FIX-029),
backend, any state/logic change.

## Acceptance

- Tag filter renders first in the home filter row, before the 正常/已归档
  status chips.
- Status toggle + tag select/clear + pagination behavior unchanged.
- No "筛选:" label reintroduced.
- `bun run check` exits 0 (`@milkdown/ctx` flake in
  `-project-issue-panel.test.tsx` is KNOWN — grep to confirm; `bun install`
  first if fresh worktree errors 127).

## Notes

- 2026-06-02 - Created at L2 xgbm1bkf (scope addition, PLAN-051).
