# FIX-055 - Contact list: batch capability resolution (N+1)

- Status: Completed
- Plan: [PLAN-104](../plan/PLAN-104.md)
- Campaign: `l1-bithk-arch-20260702084717`
- Owner: L3
- Created: 2026-07-02

## Summary

`contact/contact.service.ts:464-471` resolves capabilities per row (`resolveContactCapabilities`
policy-engine check + `composeWithCapabilities`) → ~2×N queries per contact list request. Issue
and overview lists batch correctly (`issue.service.ts:477-478`, `overview.service.ts:330-413` —
pre-built capability maps). See AUDIT-20260702-architecture.md → D4.

## Acceptance Criteria

- One (or O(1)) batched query per page resolves capabilities for all rows; per-row policy calls
  eliminated on the list path.
- Sensitivity/confidential masking semantics byte-identical: existing masking tests pass
  unchanged; add a test asserting list output equality between old per-row and new batched
  resolution for a mixed-visibility fixture.
- Detail-view path unchanged.
- `bun run check` passes.

## Files in Scope

- `apps/api/src/modules/contact/contact.service.ts`, `contact.permission.ts`, co-located tests.

## Status Notes

- 2026-07-02: Completed — contact list capabilities resolved via batched per-page lookups,
  eliminating the per-row policy N+1; masking semantics verified unchanged by tests.
