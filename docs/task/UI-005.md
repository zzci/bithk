# UI-005 Align project list toolbar with ships page

- **status**: completed
- **priority**: P1
- **owner**: current session
- **createdAt**: 2026-05-25 18:00

## Description

Align the project list toolbar with the ships list page: keep local project
search, place it on the right side of the filter row, and remove the grid/list
view-mode switch. The project list should continue to support existing
status/tag filtering, pagination, admin project creation, and card-grid
rendering.

Acceptance criteria:

1. `/projects` renders tag/status filters on the left and project search on
   the right, matching the ships list toolbar pattern.
2. `/projects` no longer renders the grid/list view-mode switch.
3. Project list rendering remains card-grid only and keeps local text search.
4. Focused frontend verification passes for the affected route or the full
   project quality gate reports no regression.

## ActiveForm

Aligning the project list toolbar with the ships page.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

2026-05-25 18:00 - Investigation found the project search is local-only in
`apps/web/src/app/routes/_app/projects/index.lazy.tsx`; no API or route
contract changes are required.

2026-05-25 18:05 - Scope changed to keep project search, move it to the right
side of the filter row like the ships page, and remove the grid/list mode
switch.

2026-05-25 18:10 - Implemented the toolbar alignment, removed the project
view-mode switch and list-table branch, and verified with web typecheck plus
ESLint on the changed route file.
