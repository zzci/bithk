# FIX-013 List-search correctness + UX polish (Campaign F)

- Status: Completed
- Plan: [PLAN-044](../plan/PLAN-044.md)
- Campaign: l1-75ymcfnr-projaudit-20260601230043
- Updated: 2026-06-01

## Goal

Push project-list search to the server `q` param (results no longer page-local)
and fix the remaining list correctness/UX nits.

## Findings

| ID | Sev | Title | Location |
| --- | --- | --- | --- |
| 01-F1 / 03-F10 | P2/P3 | Project list search only filters the current page (client-side); server `q` unused | `index.lazy.tsx:62-72,49` |
| 01-F5 / 05-F9 | P3 | `todo` status renders two different colors in the issues tab | `-project-issues-tab.tsx:63-78`; `status-colors.ts:18` |
| 01-F4 | P3 | Procurement status palette collapses two lifecycle states to one color (RE-AUDIT: suspected already fixed) | `status-colors.ts:28-36` |
| 01-F7 | P3 | A single status query failure blanks the entire issues list | `-project-issues-tab.tsx:276,349-350` |
| 01-F8 | P3 | Procurement category filter goes stale when selected category deleted | `-project-procurement-tab.tsx:166-174,302-303` |
| 01-F9 | P3 | Inline-tag cap can force a "More" trigger even when every chip fits | `-project-tag-filter.tsx:125-128` |

## Acceptance

- Project-list search uses the server `q` param; results span the whole list,
  not just the current page.
- `todo` color unified (dot vs list icon); 01-F4 palette verified/​fixed with
  evidence.
- Issues list fails closed per-status (one failing status query does not blank
  the whole list).
- Stale category filter resets when the selected category is deleted.
- Tag-cap overflow only triggers when chips actually overflow.
- `bun run check` EXIT=0.

## Scope

- In: `apps/web/.../projects/{index.lazy.tsx,-project-issues-tab.tsx,-project-procurement-tab.tsx,-project-tag-filter.tsx}`, `shared/.../status-colors.ts` + targeted tests.
- Out: performance JOIN/fan-out (Campaign B), avatar/tablist a11y (Campaign E).

## Dependencies

After B (shared `-project-issues-tab.tsx`).

## Notes

- 2026-06-02 00:0X — Dispatched as BKD L3 `8jbq8snq` (working). Deps[B] met.
  01-F7 reassigned OUT (subsumed by B's single-query collapse of the issues tab;
  L2 verifies on the integrated branch). F told not to touch the issues-tab query
  logic to avoid conflict with B's merged refactor.

## Verification (2026-06-02 — L3 8jbq8snq, cherry-picked 44239a8)

`bun run check` EXIT=0; web 566 pass, API 1224 pass / 0 fail. L3 commit-gap +
the 2 stray base commits — L2 committed (54379da) then cherry-picked only the
work commit; resolved B-vs-F conflicts on `projects.ts`/`projects.test.ts`/
`index.lazy.tsx` (combined B's `limit` cache-key with F's `q`).

- 01-F1/03-F10 DONE — list search now drives the server `q` param (debounced
  300ms), client-side page-local filter removed; `project.routes.ts` accepts `q`.
- 01-F8 DONE — procurement category filter resets when its category is deleted.
- 01-F9 DONE — tag-cap overflow reserves the "More" width only when the cap
  actually truncates (`MAX_INLINE_CHIPS=7` fed into the fit math).
- 01-F5/05-F9 ALREADY RESOLVED — `todo` is the `warning` hue everywhere:
  `status-colors.ts:18` badge `text-warning`, `STATUS_ICON_TINT.todo=text-warning`,
  `STATUS_DOT.todo=bg-warning`. No change needed.
- 01-F4 ALREADY RESOLVED — procurement palette (`status-colors.ts:26-40`) uses
  documented tint-vs-solid hue families; no two-state collision. No change needed.
- 01-F7 RESOLVED BY B — the issues tab is now a single grouped query (B/03-F2);
  the old "one of five per-status queries fails → whole list blanks" path no
  longer exists.
