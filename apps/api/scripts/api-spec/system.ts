import type { ModuleSpec } from "./types";

// OpenAPI operations for the `system` scope module. Filled by its dispatch lane;
// any route left here uncurated is auto-stubbed by the assembler so coverage
// stays at 100%. Paths use Hono `:param` form, no `/api` prefix.
export const systemSpec: ModuleSpec = {
  schemas: {},
  operations: [],
};
